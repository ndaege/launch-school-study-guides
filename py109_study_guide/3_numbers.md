There are different types of numbers in Python. The most common are integers and floats. However, there are many more. The math module allows one to import other kinds of numbers like decimals and even imaginary numbers. Numbers are subject to implicit conversions when arithmetic operations are performed on mixes. For example: 

`print(5 + 4.5) # prints 9.5, because 5 is converted to a floating point number`

